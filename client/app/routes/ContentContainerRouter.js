import React from 'react';
import { BrowserRouter, Route, Switch, Links, NavLink } from 'react-router-dom';



const ContentContainerRouter = () => (
	<BrowserRouter>
		<div>
			<Switch>
				<Route path="" component={} exact={true} />
				<Route path="" component={} />
				<Route path="" component={} />
				<Route path="" component={} />
				<Route path="" component={} />
			</Switch>
		</div>
	</BrowserRouter>
);

export default ContentContainerRouter;